# 멀티에이전트 실전 마찰 백로그 (후속 트랙 v2.4 후보 스코프)

> 2026-07-20 좌상 정리. 출처: ①material-erp 07-20 외상장부 세션 실측(메모리 fable-lite/project.md 말미 ★07-20 항목 + feedback_worker_artifact_authenticity.md 후속 규명) ②agy 검토(tmp/goals-conflict-agy.md).
> 현재 진행 중인 F2 트랙(tmp/f2-design-final.md, codex 구현 중)과 별도 — F2 반납·P4 후 착수 판단.

## 실측 사건 요약 (07-20 material-erp 4-pane)
- v2.3.0 R2 게이트가 병행 워커(agy·우하)의 산출물 쓰기를 "타 에이전트 미정산 변경"으로 fail-closed 차단·롤백 → 산출물 md 반복 소실(사본 포함), 원인 규명에 수 시간.
- 게이트 판정·deny 안내 자체는 정상 동작(agy가 안내 절차로 우회 성공). 문제는 **UX와 오탐 의심 1건**.
- 실증 기록: material-erp/.fable-lite/ gates.jsonl(pretool.contract_missing block)·coordination.jsonl(r2_deny).

## 백로그

| # | 항목 | 성격 | 상태 |
|---|------|------|------|
| B1 | **워커 산출물 회수 전 소실 UX** — 차단·롤백 대신 정산 대기 유예 또는 격리 보관함(quarantine)으로 산출물 보존 | 설계 토론 필요(R2/settlement 계열) | 대기 |
| B2 | **contract.json 등록됐는데 unresolvable_target 차단되는 케이스** | 조사 → 원인 따라 수정 | **우하 ultracode 조사 중**(tmp/r2-unresolvable-probe.md) |
| B3 | **멀티에이전트 워크플로 가이드 문서화** — 워커 계약 자가발급 레시피·해소 절차·소실 시 대응(pane 전사→좌상 파일화)을 공식 docs로 | 문서화(소형) | 대기 |
| B4 | **goals.json identity 네임스페이스** — 단일 goal 덮어쓰기 충돌(07-16 ERP 실사고+07-20 좌상 실측). agy 판정: (b) goals/<identity>.json 채택, (a)락+병합·(c)운영규약 기각(게이밍 표면·하드게이트 철학). N2 게이트(gate_counters.py:187) 동반 수정 | 설계 채택안 있음(중형) | 대기 |
| B5 | **invocations.status fail-open 원복** — PR#1 하위호환 완화(`if "status" in entry`)가 크래시는 해소했으나 고의 누락 우회 표면. agy 권고: 과거 데이터 backfill(closed) + _required 원복(fail-closed) | 마이그레이션+스키마(중형) | 대기 |

## 참고
- B2 조사 결과가 B1 설계의 입력(오탐이면 수정으로 흡수, 정탐이면 순수 UX 트랙).
- B4·B5는 F2 트랙과 파일 겹침 있음(ledger_schema·gate_counters) — F2 머지 후 착수가 안전.

## ★ 07-21 갱신 — v2.3.1 통합 스코프 확정 입력 3종 도착

1. **Sol Pro 지시서**(tmp/sol-stabilization-handoff.md): P0 5건·P1 6건·P2 3건 + PR#1~5 unresolved 리뷰 debt + PR 분할안 A~F.
2. **agy 트리아지**(tmp/stabilization-triage-agy.md): **P0 5건·P1 6건 전부 CONFIRMED**(파일:라인 근거). 특히 AUDIT-01(R2 deny가 lock 15s 대기 → hook timeout 10s 초과로 차단 응답 유실 가능)·ACT-01(root mismatch 시 B 프로젝트 무단 감독) 치명 확인.
3. **우하 B2 조사**(tmp/r2-unresolvable-probe.md): ERP "계약 있는데 unresolvable_target" 원인 3중 확정 —
   - 가설2(첫 차단): `.fable-lite` 하드차단에 셸 계약 authoring 예외 없음(설계 의도 — UX·가이드 문제, B1·B3 연결)
   - **가설3(핵심·신규)**: `_detect_dynamic_command_head`가 PowerShell `$var` 관용구를 fail-closed 차단 + 그 사유(parse_unable_dynamic_*)가 `R2_COORDINATION_REASON_MAP`(8종)에 없어 fallback `unresolvable_target`으로 **오라벨링**(코드 재현 완료). → **NEW-01**: reason map에 parse_unable_* 추가(진단 정확성) / **NEW-02**: dynamic head fail-closed의 deny 메시지 정확화·마찰 완화 검토(완화는 게이밍 표면 신중)
   - 가설1(부분): R1 계약≠R2 면제는 설계 의도 — 워커 기대와 불일치는 B3 가이드로 해소
   - 기각: 장경로(경로 짧음)·unstable_path(별도 관측 경로)·동시성 오기록(코드 구분 구조)

### v2.3.1 통합 스코프 (Sol PR 분할안 + 우리 항목 편입)
- PR A: R2-01+R2-02 (+R2-04·HINT-01 = PR E와 병합 검토)
- PR B: AUDIT-01 (+NEW-01 reason map — 같은 coordination 계열)
- PR C: ACT-01
- PR D: CODEX-01/02 (ERP 마찰 ②의 계약 불인정 경로와 동일 계열)
- PR E: R2-04+HINT-01+NEW-02
- PR F: SCORE-01+REL-01
- PR G(우리 실전): B1 소실 UX+B3 가이드 / B4 goals 네임스페이스 / B5 status 원복 — F2 머지 후
- DOC-01·PKG-01·STATE-01(ADR)은 코드 트랙 뒤.
