recommendation: REJECT

blockers:
- B1: Incomplete TaskCards fail open. `TaskCard.psm1` creates/validates mandatory `owner`, `allowed_paths`, `verify`, and `done_artifact` fields and always emits `sentinel`, but `fable_lite/card.py:46`-`fable_lite/card.py:56` silently converts missing fields to `""` or `[]`. This violates the TaskCard-as-SSOT contract.
- B2: `verify` is not required in card mode. `fable_lite/card.py:108`-`fable_lite/card.py:110` falls back to legacy verification when `card.verify` is empty. Direct CLI counterexample: a card with no `verify`, present completion files, changed `app.py`, and a stale legacy successful verification returned `fable-lite check: GREEN`, exit 0.
- B3: `allowed_paths` is not fail-closed when absent. `fable_lite/check.py:136`-`fable_lite/check.py:141` falls back to legacy prompt scope if `card.allowed_paths` is empty. Direct CLI counterexample: a card with no `allowed_paths`, changed `outside.py`, exact post-card verification, and completion files returned GREEN, exit 0.
- B4: Completion fields are not fail-closed when absent. `TaskCard.completion_paths()` only returns non-empty paths and `card_completion_findings()` checks only those paths (`fable_lite/card.py:38`-`fable_lite/card.py:42`, `fable_lite/card.py:78`-`fable_lite/card.py:85`). Direct CLI counterexample: a card with no `done_artifact` or `sentinel`, changed `app.py`, and exact post-card verification returned GREEN, exit 0.
- B5: The submitted working tree violates the explicit forbidden-directory constraint. `git diff --name-only -- core packs adapters eval` shows `adapters/claude_code/common.py`, `adapters/claude_code/post_tool_use.py`, and `eval/results/probes-latest.json`; `git ls-files --others --exclude-standard -- core packs adapters eval` shows untracked `adapters/antigravity/*` and many `eval/ab-repeat/*` files.
- B6: Scope drift remains outside the declared changed/added file list. `README.md` is modified but was not listed in the requested TaskCard integration changed-files scope.
- B7: The code-review artifact is stale and not an approval artifact for the fixed implementation. `.omo/evidence/fable-lite-v5-taskcard-code-review.md` does include `remove-ai-slops` and `programming` perspective coverage, but it records the pre-fix 6-test state and ends in `REQUEST_CHANGES`; there is no updated code-review approval covering the post-fix 9-test state.

originalIntent:
Add `python -m fable_lite check --card <card.json>` and `python -m fable_lite brief --card <card.json>` for wmux TaskCard JSON. The card fields `owner`, `allowed_paths`, `forbidden_paths`, `verify`, `done_artifact`, and `sentinel` should drive delegation and checking. Card `allowed_paths` must drive scope; `forbidden_paths` violations must be detected; the exact verify command must be required from post-card evidence; `done_artifact` and `sentinel` existence must be checked without treating them as verification; no-card legacy modes must keep working; README gets one card usage line; tests include a card RED scenario. The user explicitly forbids edits under `core/`, `packs/`, `adapters/`, and `eval/`.

desiredOutcome:
The user can hand a real TaskCard generated by `TaskCard.psm1` to `brief --card` and `check --card` and receive a reliable fail-closed gate. Valid cards with exact post-card verification and required completion paths pass; incomplete cards, forbidden edits, scope escapes, stale verification, and missing completion paths fail. The implementation diff stays inside the approved TaskCard files and avoids forbidden directories.

userOutcomeReview:
The main happy path and the three reported fixes are present: valid card check can pass, forbidden matching is casefolded, allowed globs are checked directly, post-card agent JSONL exact verification is required when `verify` exists, and `brief --card` now lists both completion paths when both are present. However, from the user's perspective the artifact is not reliable enough to ship because incomplete cards can still pass as GREEN. That undermines the TaskCard contract exactly where the gate should be strict. The worktree also contains forbidden `adapters/` and `eval/` changes, so the user's scope constraint is not satisfied.

checkedArtifactPaths:
- `C:/Users/rotat/.claude/scripts/TaskCard.psm1`
- `fable_lite/card.py`
- `fable_lite/check_support.py`
- `fable_lite/check.py`
- `fable_lite/brief.py`
- `fable_lite/cli.py`
- `tests/test_fable_lite_cli.py`
- `README.ko.md`
- `README.md`
- `tmp/codex-ulw-done9-notepad.md`
- `.omo/evidence/fable-lite-v5-taskcard-code-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-cli-gate-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-security-code-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-context-gate-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-cli/tc-brief-card.txt`
- `.omo/evidence/fable-lite-v5-taskcard-cli/tc-check-card-red.txt`

verificationRun:
- `python -m pytest tests/test_fable_lite_cli.py -q` -> 9 passed.
- `python -m pytest tests/ -q` -> 50 passed.
- `python -m py_compile fable_lite/card.py fable_lite/check_support.py fable_lite/check.py fable_lite/brief.py fable_lite/cli.py tests/test_fable_lite_cli.py` -> exit 0.
- `python -m basedpyright --level error fable_lite tests/test_fable_lite_cli.py` -> 0 errors, 0 warnings, 0 notes.
- Pure LOC: `card.py` 165, `check_support.py` 79, `check.py` 175, `brief.py` 65, `cli.py` 25, `tests/test_fable_lite_cli.py` 191.
- Manual valid card scenario -> GREEN, exit 0.
- Manual missing `verify` scenario -> GREEN, exit 0; expected RED.
- Manual missing `allowed_paths` scenario -> GREEN, exit 0; expected RED.
- Manual missing `done_artifact`/`sentinel` scenario -> GREEN, exit 0; expected RED.

directRemoveAiSlopsAndProgrammingPass:
- `remove-ai-slops` lens: No deletion-only, tautological, or pure implementation-mirroring tests were found in the new card tests. The remaining issue is false confidence from missing adversarial classes: tests cover wrong verify, case mismatch, glob broadening, and pre-card verify, but not absent mandatory card fields.
- `programming` lens: The TaskCard JSON boundary is parsed into a dataclass but not validated into a valid value. Missing required fields are represented as empty strings/lists, violating parse-don't-validate and letting downstream legacy fallback paths approve incomplete cards.
- Production extraction into `card.py` and `check_support.py` is not an oversized-file workaround; current pure LOC stays below the 250-line ceiling. The extraction itself is not a blocker.

codeReviewReportCoverage:
- Present but unsupported for approval. `.omo/evidence/fable-lite-v5-taskcard-code-review.md` explicitly mentions `remove-ai-slops` and `programming` perspectives and overfit/slop concerns, but it is a stale pre-fix report and recommends `REQUEST_CHANGES`. It cannot support post-fix approval.

exactEvidenceGaps:
- No updated post-fix code-review approval artifact.
- No test for missing mandatory TaskCard fields (`verify`, empty/missing `allowed_paths`, missing completion fields).
- Manual QA evidence in `.omo/evidence/fable-lite-v5-taskcard-cli/` is incomplete for the post-fix state; the stored `tc-brief-card.txt` is mojibake and does not prove the final both-path brief behavior.
- The declared changed/added file list excludes `README.md`, but `README.md` is dirty.
- The user-forbidden `adapters/` and `eval/` paths are dirty/untracked in the current worktree.

notes:
- Positive: legacy no-card tests still pass.
- Positive: README.ko.md contains one card usage line.
- Positive: the reported post-fix regressions for case-insensitive forbidden matching, allowed glob broadening, and pre-card legacy verification are covered by tests and pass.
- Negative: Passing tests do not cover the fail-open required-field classes, and current CLI behavior confirms those classes pass incorrectly.
