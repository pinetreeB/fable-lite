# fable-lite v5 TaskCard Final Context Mining Re-review Gate

recommendation: APPROVE

## blockers

None.

## originalIntent

Re-review the fable-lite v5 TaskCard integration after the malformed-card fix, in read-only mode, and decide whether any local TaskCard/wmux context was still missed. The local context blockers to verify were:

- `brief --card` must tell workers to create both `done_artifact` and `sentinel` when both are present in a TaskCard generated by `TaskCard.psm1`.
- Incomplete TaskCard JSON must fail closed: `check --card` must report RED under `작업카드 오류`, and `brief --card` must reject invalid cards.
- The previously verified fixes should remain present: direct allowed-path glob enforcement, casefold forbidden matching, post-card exact verify only, and removal of stale root-scope helpers.

## desiredOutcome

For a valid TaskCard from `C:/Users/rotat/.claude/scripts/TaskCard.psm1`, `brief --card` and `check --card` should use the card as the local source of truth: `owner`, `allowed_paths`, `forbidden_paths`, `verify`, `done_artifact`, and `sentinel` drive the user-visible delegation/check behavior. For malformed cards missing mandatory TaskCard fields (`slug`, `owner`, non-empty `allowed_paths`, `verify`, `done_artifact`), the CLI should fail closed rather than silently falling back to legacy prompt/ledger behavior.

## userOutcomeReview

PASS. The current implementation satisfies the requested local-context outcome from the user's perspective.

Evidence from the current scoped files:

- `TaskCard.psm1` confirms generated cards contain `slug`, `owner`, `allowed_paths`, `forbidden_paths`, `verify`, `done_artifact`, and `sentinel`; its conflict check treats `slug`, `owner`, `verify`, `done_artifact`, and non-empty `allowed_paths` as mandatory.
- `fable_lite/card.py` now has `card_validation_findings()` enforcing `slug`, `owner`, `verify`, `done_artifact`, and non-empty `allowed_paths`.
- `fable_lite/check.py` includes `card_messages` in `CheckResult.is_green()`, so invalid cards force RED; manual malformed-card QA returned exit 1.
- `fable_lite/brief.py` calls `_reject_invalid_card()` before rendering and uses `card.completion_paths()`, so invalid cards are rejected and valid cards with both completion paths print both.
- `tests/test_fable_lite_cli.py` includes the malformed-card regression test and retained the prior edge tests for forbidden casefold, direct allowed glob, stale legacy verify, and both-path brief output.

## checked artifact paths

- `C:/Users/rotat/.claude/projects/C--Users-rotat/memory/MEMORY.md`
- `C:/Users/rotat/.claude/scripts/TaskCard.psm1`
- `docs/design/wmux-orchestration.md`
- `fable_lite/card.py`
- `fable_lite/check_support.py`
- `fable_lite/check.py`
- `fable_lite/brief.py`
- `fable_lite/cli.py`
- `tests/test_fable_lite_cli.py`
- `README.ko.md`
- `tmp/codex-ulw-done9-notepad.md`
- `.omo/evidence/fable-lite-v5-taskcard-code-quality-rereview-code-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-context-rereview-gate-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-integration-gate-review.md`
- `.omo/evidence/fable-lite-v5-taskcard-security-rereview-code-review.md`

## direct verification

- `python -m pytest tests/test_fable_lite_cli.py -q` -> PASS, 10 passed.
- `python -m pytest tests/ -q` -> PASS, 51 passed.
- `python -m py_compile fable_lite/card.py fable_lite/check_support.py fable_lite/check.py fable_lite/brief.py fable_lite/cli.py tests/test_fable_lite_cli.py` -> PASS, exit 0.
- `python -m basedpyright --level error fable_lite tests/test_fable_lite_cli.py` -> PASS, 0 errors, 0 warnings, 0 notes.
- Manual malformed `check --card` with only `owner` and `forbidden_paths` -> RED, exit 1, with `작업카드 오류` and missing `slug`, `verify`, `done_artifact`, `allowed_paths`.
- Manual invalid `brief --card` with the same malformed card -> exit 1 with `작업카드 오류`.
- Manual valid `brief --card` with `done_artifact="tmp/.done-green"` and `sentinel="tmp/.sentinel-green"` -> exit 0 and printed both completion paths.
- Manual valid `check --card` with post-card exact `verify`, allowed `app.py`, and both completion files -> GREEN, exit 0.
- Manual missing-owner, missing-slug, and empty-allowed-path cards -> RED, exit 1.
- `rg -n "scope_roots|_scope_root" fable_lite tests` -> no matches; stale root helper is removed.

## remove-ai-slops and programming pass

- Loaded and applied `remove-ai-slops` and `programming` plus the Python reference README as review lenses.
- No deletion-only tests, tautological tests, implementation-call-mirroring tests, skipped tests, `xfail`, `.only`, `type: ignore`, `pyright: ignore`, `Any`, `cast()`, broad `except`, or stale `scope_roots/_scope_root` helper remained in the scoped files.
- The malformed-card test checks CLI-observable RED output and exit code, not an internal helper call.
- The code-quality rereview artifact explicitly records `programming` and `remove-ai-slops` coverage; it predates the final malformed-card test-count change, so this gate independently re-ran the same slop/overfit pass over the current diff.

## exact evidence gaps

No blocking evidence gaps for the requested context-mining verdict.

Non-blocking notes:

- Some manual PowerShell stdout captures rendered Korean as mojibake in the tool transcript, but exit codes and ASCII/Korean source-level checks were verified. The pytest assertions also exercise the Korean `작업카드 오류` path.
- Dirty worktree entries outside the listed implementation scope remain present (`adapters/`, `eval/`, `README.md`, etc.). Per the user's instruction, these were treated as residual worktree state because the scoped implementation files and current diff evidence do not show they are blockers for this TaskCard review.

