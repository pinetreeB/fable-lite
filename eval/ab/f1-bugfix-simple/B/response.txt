조사 팩 마커를 갖춰 다시 정리합니다.

**재현:** `python demo.py` → `2 + 3 = -1` (기대 5).

**가설 1:** `add`가 `-` 연산자로 뺄셈을 수행한다.
**증거:** `calc.py:2`에 `return a - b`. 2 - 3 = -1 로 관측값과 정확히 일치. → 채택.

**가설 2:** `demo.py`가 인자를 잘못 전달한다.
**증거:** `demo.py:2`는 `add(2, 3)`으로 올바르게 호출. → **기각:**

**가설 3:** import가 다른 `add` 정의를 가져온다.
**증거:** 저장소 전체에서 `add` 정의는 `calc.py` 하나뿐(grep 확인). → **기각:**

**수정:** `calc.py:2` `return a - b` → `return a + b`
**검증:** `python demo.py` → `2 + 3 = 5` ✅ (수정 전 `-1`, 수정 후 `5`로 관측 확인)